[package]
name = "surveillance_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "surveillance_core"
crate-type = ["cdylib"]

[dependencies]
# The "features" part is critical!
pyo3 = { version = "0.19.0", features = ["extension-module"] }
numpy = "0.19.0"